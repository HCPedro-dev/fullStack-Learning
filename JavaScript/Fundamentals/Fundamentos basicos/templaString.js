const nome = 'Pedro'
const concatenar = 'ola ' + nome + '!' // concatenação normal
const concatenarTemplateString = `Olá, ${nome}!`// template string normalmente utilizado para concatenar strings
console.log(concatenar, concatenarTemplateString) // mostra os resultados das concatenações


// epressoes 
console.log(`1 + 1 = ${1 + 1}` ) // soma